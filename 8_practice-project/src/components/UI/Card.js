import styled from 'styled-components';

const Card = styled.div`
    padding: 1.2em;
    border-radius: 1.2em;
    background-color: rgb(250, 250, 250);
    width: fit-content;
    height: fit-content;
`

export default Card;